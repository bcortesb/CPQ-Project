version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
    depends_on:
      - db

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./frontend:/app

  db:
    image: postgres:13
    environment:
      POSTGRES_USER: cpq-user
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: cpqDb
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
